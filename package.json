{
  "name": "reikna",
  "source": "src/index.html",
  "scripts": {
    "lint": "eslint --ignore-path .eslintignore --ext .js,.ts ./src/.",
    "clean": "rm -rf ./dist",
    "tsc": "tsc --noEmit",
    "dev": "parcel",
    "build": "yarn lint && yarn tsc && yarn clean && parcel build",
    "start": "serve -s dist"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.1",
    "@parcel/validator-typescript": "^2.0.1",
    "@typescript-eslint/eslint-plugin": "^5.5.0",
    "@typescript-eslint/parser": "^5.5.0",
    "eslint": "^8.3.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-jsx-a11y": "^6.5.1",
    "parcel": "^2.0.1",
    "prettier": "^2.5.0",
    "serve": "^13.0.2",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  }
}
